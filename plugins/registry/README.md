# Plugin Registry

Foundation for discovering and enabling optional modules without modifying core.

- Manifest schema and registration process: see `docs/architecture/PLUGIN_FOUNDATION.md`
- Plugins remain **disabled** until feature flags and plan entitlements allow them
- Do not implement plugin runtimes in the foundation phase
