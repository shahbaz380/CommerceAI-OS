# SKU & Variants

- SKUs normalized to uppercase
- Unique per workspace across products.default_sku and product_variants.sku
- Auto-SKU via `auto_sku=true` on product create
- Variants hold option_values JSON (e.g. color/size)
