# Categories

Tenant-owned hierarchy with `parent_id`, `path`, `depth`.

- Circular parent references rejected
- Tree endpoint: `GET /api/v1/product-categories/tree`
- Marketplace taxonomy mapping stored in `marketplace_mapping` JSON placeholder
